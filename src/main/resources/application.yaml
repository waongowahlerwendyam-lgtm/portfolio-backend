server:
  port: 8080

spring:
  mail:
    host:  smtp-relay.brevo.com
    port: 587
    username: waongowahlerwendyam@gmail.com
    password: ${SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true